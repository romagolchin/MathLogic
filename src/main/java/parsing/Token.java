package parsing;

/**
 * @author Roman Golchin (romagolchin@gmail.com)
 */
public enum Token {
    OR, AND, OPEN, CLOSE, NOT, IMPL, VAR, INC, END,
    ADD, MUL, EQ, FORALL, EXISTS, COMMA, PRED, ZERO, FUNC, PROV
}
